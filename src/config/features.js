/**
 * Feature flags: set to false to hide Drawing and Annotation UI
 * while preserving the underlying logic for future use.
 */
export const FEATURES_ANNOTATION_DRAWING_HIDDEN = true

/** Set to false to show profile mode (Create Profile, My Profile, ProfileSheet). */
export const FEATURES_PROFILE_UI_HIDDEN = true

/** Set to false to show Admin Dashboard button(s) in the header. */
export const FEATURES_ADMIN_UI_HIDDEN = true
